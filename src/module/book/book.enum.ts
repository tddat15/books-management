export const index = 'books';

export enum BookOrder {
  id = 'id',
  title = 'title',
  author = 'author',
  price = 'price',
  publishedDate = 'publishedDate',
}

export enum Dimension {
  asc = 'asc',
  desc = 'desc',
}
